"""Adapter helpers for invoking an agent under test.

Why this file exists:
It keeps adapter concerns grouped under one package even though the MVP only
supports a single command-based adapter style.
"""
